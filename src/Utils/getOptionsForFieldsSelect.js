
// const getOptions = ((data, value) => {

// export const getOptionsForFieldsSelect = (data, value) => {
  
  // return () => {
  //   const result = data.filter(el => el.name === value)[0]?.fields.filter(el => el.name).reduce((arr, el) => {
  //     arr.push({label: el.name, value: [el.name, el.id, el.type]});
  //     return arr
  //   }, [])
  //   if (result) {
  //     return result
  //   } else {
  //     const result = []
  //     data.forEach((model) => {
  //       model.fields.forEach(field => result.push({label: field.name, value: [field.name, field.id, field.type]}))
  //     })
  //     return result
  //   }
  //   console.log(result, 'result')
  // }
  // }
  //